import styled from "styled-components";

export const BoardBackground = styled.div`
    background: var(--blue);
    width: 715px;
    height: 625px;
    border-radius: 10px;
    margin: 20px auto;
`;